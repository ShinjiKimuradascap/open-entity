[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.roulette"

[deploy]
startCommand = "python services/ai_roulette_api.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
